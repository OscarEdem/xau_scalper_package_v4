# Tell linguist to ignore Cargo.lock for language statistics
# It's a generated file and skews the repository's language breakdown.
rust-server/Cargo.lock linguist-vendored

# Ensure .mk files are correctly identified as Makefiles.
*.mk linguist-language=Makefile

# Force LF line endings for SQL files to ensure consistent sqlx checksums
*.sql text eol=lf